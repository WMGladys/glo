import 'package:flutter/material.dart';

class MakeupPage extends StatelessWidget {
  const MakeupPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('This is the makeup shop page'),
      ),
    );
  }
}
